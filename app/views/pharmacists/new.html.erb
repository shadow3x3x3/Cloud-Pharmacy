<%= form_for @pharmacist, :url  => pharmacists_path,
                          :html => { :class => 'form-horizontal' } do |f| %>
  <h1>新增藥師資料</h1>

  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= f.submit "Create", class: "btn btn-default btn-primary" %>

<% end %>