<%= form_for @hospital, :url  => hospitals_path,
                        :html => { :class => 'form-horizontal' } do |f| %>
  <h1>新增醫院資料</h1>

  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= f.submit "Create", class: "btn btn-default btn-primary" %>

<% end %>