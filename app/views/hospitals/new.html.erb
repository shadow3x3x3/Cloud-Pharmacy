<%= bootstrap_form_for @hospital, :url  => hospitals_path do |f| %>
  <h1>新增醫院資料</h1>

  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= f.submit "建立", class: "btn btn-primary" %>
  <%= link_to "取消", hospitals_path, :class => "btn btn-default" %>

<% end %>