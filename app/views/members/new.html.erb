<%= bootstrap_form_for @member, :url => members_path do |f| %>
  <h1>新增會員資料</h1>

  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= f.submit "建立", class: "btn btn-primary" %>
  <%= link_to "取消", members_path, :class => "btn btn-default" %>

<% end %>